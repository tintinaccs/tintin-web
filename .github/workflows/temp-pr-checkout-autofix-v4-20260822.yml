name: TEMP checkout patch source noop

on:
  workflow_dispatch:

jobs:
  source:
    runs-on: ubuntu-latest
    steps:
      - name: Fuente auxiliar
        shell: bash
        run: |
          python3 - <<'PY'
          # Fuente auxiliar intencionalmente vacía. La corrección final vive
          # en temp-pr-checkout-autofix-20260822.yml y esta segunda fuente se
          # conserva únicamente porque la ejecución validada lee ambas.
          pass
          PY
