# tintinaccesorios.pages.dev pasó a ser el dominio real del sitio (antes
# solo corría las funciones /api/* de atrás). Ya no redirige a nadie —
# sirve el sitio completo tal cual, incluida la ficha de autenticación de
# Firebase por functions/__/auth/[[path]].js (ver js/firebase.js:
# authDomain). _routes.json sigue mandando /api/* y /__/auth/* a sus
# funciones antes de llegar a cualquier archivo estático.
